
<section class="section page-width not-found">
  <h1>{{ '404.title' | t }}</h1>

  <p>{{ '404.not_found' | t }}</p>

  <a
    class="btn btn--primary"
    href="{{ routes.all_products_collection_url }}"
  >
    {{ '404.back_to_shopping' | t }}
  </a>
</section>

{% schema %}
{
  "name": "t:general.404",
  "settings": []
}
{% endschema %}
